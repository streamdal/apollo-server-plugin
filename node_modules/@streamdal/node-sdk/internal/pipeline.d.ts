import { Command, TailCommand } from "@streamdal/protos/protos/sp_command";
import { Audience } from "@streamdal/protos/protos/sp_common";
import { Pipeline } from "@streamdal/protos/protos/sp_pipeline";
import { InternalConfigs } from "../index.js";
export declare const initPipelines: (configs: InternalConfigs) => Promise<void>;
export declare const processResponse: (response: Command) => Promise<void>;
export declare const buildPipeline: (pipeline: Pipeline) => Promise<Pipeline>;
export declare const setPipelines: (audience: Audience, pipelines: Pipeline[]) => Promise<void>;
export declare const tailPipeline: (audience: Audience, { request }: TailCommand) => void;
