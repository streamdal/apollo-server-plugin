import { IInternalClient } from "@streamdal/protos/protos/sp_internal.client";
export declare const HEARTBEAT_INTERVAL = 1000;
export interface HearbeatConfigs {
    serviceName: string;
    grpcClient: IInternalClient;
    sessionId: string;
    streamdalToken: string;
}
export declare const heartbeat: ({ serviceName, grpcClient, sessionId, streamdalToken, }: HearbeatConfigs) => Promise<void>;
