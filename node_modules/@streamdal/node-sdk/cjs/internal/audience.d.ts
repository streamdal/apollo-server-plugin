import { Audience } from "@streamdal/protos/protos/sp_common";
import { InternalConfigs } from "../index.js";
export interface AddAudience {
    configs: InternalConfigs;
    audience: Audience;
}
export declare const addAudiences: (configs: InternalConfigs) => Promise<void>;
export declare const addAudience: ({ configs, audience }: AddAudience) => Promise<void>;
