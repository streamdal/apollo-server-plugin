export declare const SAMPLE_DEFAULT: {
    sampleRate: number;
    sampleIntervalSeconds: number;
};
export declare class TokenBucket {
    private sampleRate;
    private tokens;
    private sampleIntervalSeconds;
    private lastRefill;
    constructor(sampleRate: number | undefined, sampleIntervalSeconds: number | undefined);
    private refill;
    consume(): boolean;
}
